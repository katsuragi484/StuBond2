<div class="container">
  <div class="form-group row">
    <div class="col">
      <h3 class="mt-2">教科編集</h3>
    </div>
  </div>
<%= form_with model: @subject, url: admin_subject_path(@subject.id) do |f| %>
  <%= f.label :教科名 %>
  <%= f.text_field :subject_name %>
  <%= f.submit "変更を保存", class: "btn btn-success"%>
<% end %>
</div>