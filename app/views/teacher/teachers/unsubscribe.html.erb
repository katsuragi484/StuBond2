<div class='container mt-5'>
    <div class="container text-center mt-4">
      <div class="col-xs-offset-1">
        <h3>
          <strong>本当に退会しますか？</strong></br>
        </h3>
          <div>
            退会する場合は、「退会する」をクリックしてください。
          </div>
      </div>
    </div>
    <div class="container text-center mt-4">
      <div class="col-xs-offset-1">
        <%= link_to "退会しない", :back, class: 'btn btn-outline-success' %>
        <%= link_to "退会する", withdraw_teacher_teachers_path, method: :patch, data: { confilm: "本当に退会しますか？" }, class: 'btn btn-danger' %>
      </div>
    </div>
</div>